# Source

`src/` is the public workflow module lane for `uDOS-empire`.

Current source surfaces include:

- `workflow-pattern.json` as the smallest checked-in workflow contract

Boundary rule:

- keep public business and publishing contracts here
- keep private OMD behavior outside this repo
