# uDOS-empire Getting Started

1. Read `docs/boundary.md`.
2. Review `docs/activation.md`.
3. Keep new modules public and source-first.
4. Run `scripts/run-empire-checks.sh`.
5. Put runnable demonstrations in `examples/`.
6. Add regression tests for any workflow contract.
