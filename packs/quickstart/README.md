# Quickstart

`quickstart` is a starter orientation pack for first-time `uHOME-server`
operators using `uHOME-empire` packs.

It demonstrates:

- preview-first understanding of inputs and outputs
- a minimal reviewed workflow hosted by `uHOME-server`
- pack structure that users can inspect before moving to richer packs
